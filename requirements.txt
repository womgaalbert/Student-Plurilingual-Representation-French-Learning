# French-Learning-Perceptions ML — requirements.txt
# Streamlit Cloud : dependances legeres pour chargement .pkl uniquement
# Requirements complets pour dev local : voir deploy_cloud/requirements-full.txt

# Dashboard
streamlit>=1.28

# Core ML (chargement modeles .pkl)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=1.7
imbalanced-learn>=0.11
scipy>=1.11
pyyaml>=6.0

# MLflow (tracking)
mlflow>=2.8

# Tests (CI)
pytest>=7.4
pytest-cov>=4.1

# Visualisation (base)
matplotlib>=3.7
seaborn>=0.12
