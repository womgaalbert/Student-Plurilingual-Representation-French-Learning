# French-Learning-Perceptions ML — requirements.txt
# Streamlit Cloud : dependances legeres pour chargement .pkl uniquement
# Requirements complets pour dev local : voir deploy_cloud/requirements-full.txt

# Dashboard
streamlit>=1.28

# Core ML (chargement modeles .pkl) — versions sync avec entraînement
pandas>=2.0
numpy>=1.24
scikit-learn==1.6.1
xgboost==3.2.0
lightgbm==4.6.0
imbalanced-learn==0.14.1
scipy==1.17.1
pyyaml>=6.0
scipy>=1.11

# MLflow (tracking)
mlflow>=2.8

# Tests (CI)
pytest>=7.4
pytest-cov>=4.1

# Visualisation (base)
matplotlib>=3.7
seaborn>=0.12
