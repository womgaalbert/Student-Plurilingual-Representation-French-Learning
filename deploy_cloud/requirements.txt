# requirements-cloud.txt — Streamlit Community Cloud deployment
# Lightweight: no torch, no transformers, no spacy
# The app uses pre-trained .pkl models (sklearn/xgboost) for predictions

streamlit>=1.28
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=3.0
pyyaml>=6.0
imbalanced-learn>=0.11
scipy>=1.11
